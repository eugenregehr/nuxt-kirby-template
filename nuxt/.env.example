# URL of the Kirby backend
KIRBY_BASE_URL=http://localhost:8000

# Bearer token for the KQL API.
# Must match KIRBY_HEADLESS_API_TOKEN in the backend's .env.
KIRBY_API_TOKEN=

# Canonical URL of this frontend (used for SEO meta, sitemap, robots)
NUXT_PUBLIC_SITE_URL=http://localhost:3000
