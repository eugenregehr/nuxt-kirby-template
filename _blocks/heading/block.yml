# `builtin: true` — Kirby ships the blueprint for this block type, so this
# package only contributes the frontend component and its types.
baukasten:
  category: text
  order: 10
  builtin: true

# Label and icon for the block palette — the fields come from Kirby's own blueprint
name:
  en: Heading
  de: Überschrift
icon: title
